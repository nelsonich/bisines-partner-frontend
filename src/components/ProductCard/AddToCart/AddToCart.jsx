import React from 'react';
import { BsCart2 } from 'react-icons/bs';

function AddToCart(
  {
    /* product */
  }
) {
  // const filtredLocalStorageItems = [flower].filter((item) => {
  //   return item.id === flower.id;
  // });

  return (
    <div>
      <BsCart2
      // onClick={addToCart}
      />
    </div>
  );
}
export default AddToCart;
